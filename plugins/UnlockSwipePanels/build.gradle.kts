version = "1.0.1"
description = "Stops the swipe one direction on a stacked swipe panel from blocking a swipe the other way"

aliucord {
    changelog.set(
        """
        # 1.0.1
        * Removed some reflection things.
            
        # 1.0.0
        * Initial plugin release!
        """.trimIndent(),
    )

    deploy.set(true)
}
